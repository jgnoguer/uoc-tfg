apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: mediastorage
  namespace: default
spec:
  template:
    spec:
      nodeSelector:
        longhorn: compatible
      containers:
        - image: ghcr.io/jgnoguer/mediastorage:latest
          resources: {}
          volumeMounts:
            - mountPath: /data
              name: mydata
              readOnly: false
          env:
            - name: SCYLLADB_IP
              valueFrom:
                configMapKeyRef:
                  name: uoc-animals-services-cm
                  key: scylladb-ip
            - name: SCYLLA_APPUSER
              valueFrom:
                secretKeyRef:
                  name: uoc-animals-database-creds
                  key: username
            - name: SCYLLA_APPPWD
              valueFrom:
                secretKeyRef:
                  name: uoc-animals-database-creds
                  key: password
            - name: STORAGE_FOLDER
              value: "/data"
      volumes:
        - name: mydata
          persistentVolumeClaim:
            claimName: noreplicavolume
            readOnly: false
